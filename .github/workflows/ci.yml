name: CI

on:
  push:
    branches: [main, dev]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
      - run: uv sync --all-packages
      - name: Lint (library, tests, dsl)
        # Research-grade zones (engine/experiments, engine/datasets) carry
        # explicit per-file-ignores in pyproject: real bugs block, style
        # pedantry does not.
        run: uv run ruff check engine tests dsl
      - name: Type check
        run: uv run mypy engine
      - name: Tests
        run: uv run pytest tests dsl/tests
