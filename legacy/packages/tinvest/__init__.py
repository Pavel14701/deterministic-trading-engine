"""dte-tinvest: T-Invest (Tinkoff Invest) candle history adapter.

Market-data only: the REST client, instrument/bar mapping and the
canonical OHLCV frame consumed by the dataset pipeline via the shared
``marketdata`` source registry.
"""
